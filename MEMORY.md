# MEMORY — Cross-Session Project State

This is a working log for agents (Kimchi and others) that don't retain context between sessions. Read the **Current state** section and the last 3-5 **Log** entries before starting work; append one short entry when you finish a task. This is not a changelog for humans — `git log` is authoritative for that. Keep entries short and factual; prune stale ones instead of letting this file grow unbounded (target: keep it under ~150 lines — archive older log entries into `requirements-specs/**/INDEX.md` progress logs where they already live, don't duplicate).

## Current state (as of 2026-07-06)

- **Stack:** FastAPI + Celery + Postgres backend, Next.js (App Router, React 19) frontend, Docker-first. No migration tool — schema changes apply via `Base.metadata.create_all` on app start (see `SKILLS.md` §2).
- **Core platform capabilities shipped:** Audit Trail, Schema Drift Detection (Celery beat), persistent Query History/Chat Sessions, JWT auth, AI Autopilot backend (7-step agent loop). See `backend/app/models/` and `backend/app/api/routers/` for the concrete surfaces.
- **Schema Mapper** — the old `requirements-specs/mapper_tasks/INDEX.md` referenced here no longer exists (superseded); current Schema Mapper work lives under `requirements-specs/enterprise_v2/` (E04 AI mapping intelligence, E10 workbench, E13 review workflow, E14 feedback loop, E15 docs/reports, E16 annotations — see that dir's `INDEX.md`) plus dated one-off bug reports like `schema_mapper_bugs_2026-07-22.md` in the same dir. #7-equivalent tenant isolation sign-off is still blocked on product (see `requirements-specs/tenant_isolation_tasks/00_architecture_decision.md`).
- **Pipelines epic** (`requirements-specs/Pipelines_tasks/INDEX.md`): tasks defined (data model, drift validation, execution engine, scheduler, retry handling, run history, UI monitoring, audit/role gating, concurrency, tests, secret vaulting) — check that INDEX.md for current status before assuming any are done.
- **Governance files added 2026-07-06:** root `CLAUDE.md` (entrypoint), `SKILLS.md` (task playbooks), this file. `prompts/*.md` remains the binding coding-standards contract and was intentionally left unmodified in that pass.
- **Dashboard epic** (`requirements-specs/dashboard_tasks/INDEX.md`): tasks #1–#7 done 2026-07-07 (aggregation API + cache + role-scoping + widget framework + tiles/feed/range-filter); #8 backend tests done, frontend component tests blocked on missing jest/RTL infra (human call); #9 tenant sign-off blocked on product.
- **Connectors epic** (`requirements-specs/connector_tasks/INDEX.md`): tasks #1, #3–#7 done 2026-07-07 (soft-delete data model + ConnectionService, types catalog + config validation, test diagnostics w/ 5s timeout across all 5 drivers, beat health checks, discovery handoff, dependency-aware delete/restore/hard-delete; plus a hotfix — connector reads were unauthenticated and returned plaintext passwords; now auth-gated + secret-redacting). #2 (secret vaulting) and #8 (edit/rotate) blocked on encryption-approach sign-off; #10 tenant sign-off blocked on product. 6/9 FRs done.

## Known constraints / gotchas

- `SECRET_KEY` (JWT) must be overridden via env var in production — code default is dev-only.
- No Alembic-equivalent migrations — destructive schema changes (drop/rename column) need a human sign-off, not an auto-implementation.
- Celery beat requires its own `beat` service entry in `docker-compose.yml` — periodic tasks silently won't fire without it.
- `frontend/src/lib/api.ts` is the single chokepoint for HTTP calls and 401 handling (`setUnauthorizedHandler`) — don't add a parallel fetch or auth-redirect path.
- `ApiError.message` is built by `api.ts`'s `errorMessageFromDetail()` from the response's `detail`. If a backend endpoint raises `HTTPException(detail={...})` with a structured dict (several mapping endpoints already do, e.g. `{kind, message, issues}`), always include a top-level string `message` field (or an `issues: [{message}]` list) — anything else falls back to `JSON.stringify`, which is legible but not a designed UI string.

## Log

- 2026-07-22 — Same-day follow-up to the 4-bug Schema Mapper pass below: 2 feature requests, full detail in `requirements-specs/enterprise_v2/schema_mapper_bugs_2026-07-22.md` Part 2. (5) **AI Suggestions is now a mode toggle** ("Manual" / "AI Suggested") in the page header, built by directly reusing Query Workspace's own Ask/SQL mechanism (`SegmentedControl`, both views permanently mounted and swapped via a `hidden` class, a background-completion indicator dot) rather than inventing a new pattern — Canvas and SuggestionPanel are now full-height siblings instead of AI Suggestions being squeezed into a small panel below the canvas. (6) **Added a real threshold-conditional transformation kind (`case`)** — end-to-end: grammar/schema/SQL-compile, the Python preview evaluator, frontend types/UI (an operator dropdown + compare/then/else fields with numeric-vs-string auto-coercion so typing `500000000` doesn't silently become the string `"500000000"`), and, more consequentially, **`pipeline_executor.py`**: it previously executed *only* `kind == "direct"` transformations for real (not just the new kind — `upper`/`cast`/etc. never executed either, despite previewing fine), which would have shipped `case` as preview-only. Now every single-source kind that `transformation_preview_service.apply_transformation_preview` already evaluates safely reuses that exact function as a per-row Python step in `_batch_copy` (skipped entirely, zero overhead, when a batch is all-`direct`) — `concat` (multi-source) and `lookup` (live join) remain the only execution-blocked kinds, both needing real design work not attempted here. Grounding: the two example transformations the user asked about were *already sitting in* `E2E_DEMO_WALKTHROUGH.md` as an aspirational bracket-notation placeholder from an earlier session — nobody had built the capability the doc described. Updated that doc with a real, followable worked example, replacing the placeholder. Verified backend 1044/1044 (16 new tests across grammar/preview/execution-engine), frontend `tsc`/ESLint clean, vitest clean (330 passing, same pre-existing unrelated `QueryWorkspaceInner` failures as before), production build clean. Live-verified the exact two walkthrough edges (`accounts.annual_revenue → dim_customer.segment`, `accounts.employee_count → dim_customer.is_active`) via the real preview endpoint against real seeded data — deliberately did **not** run a real pipeline against the shared demo `dim_customer` table, since its 10 existing seeded rows' `segment` values don't all agree with a live $500M cut and a real run would have silently altered them. **Uncommitted.**

- 2026-07-22 — Fixed 4 user-reported Schema Mapper bugs found by hands-on testing of the live app (not a code-review pass); full detail in `requirements-specs/enterprise_v2/schema_mapper_bugs_2026-07-22.md`. (1) **Publish looked stuck**: `api.ts` built `ApiError.message` from `body.detail` unchecked — several mapping endpoints (publish's blocking-validation 422 among them) send a *structured* `detail` object, and JS's `Error` constructor silently stringifies a non-string message to `"[object Object]"`; added `errorMessageFromDetail()` (string / FastAPI validation array / `{message}` / `{issues:[{message}]}`, else `JSON.stringify`) shared by all 8 `api.ts` verbs, plus a persistent in-dialog error banner and a silent validation-refresh on a 422 publish failure (dialog counts were stale from the last manual Validate click). (2) **Canvas too small**: `ValidationPanel`'s close button was wired to a no-op in `page.tsx`; `SuggestionPanel` had no collapse. Added a real `clearValidation()` action, a `SuggestionPanel` collapse toggle, and a page-level "Focus mode" that hides the mapping-list rail + collapses side panels for display only (doesn't overwrite saved `workspaceLayout` state) — considered and rejected a literal `window.open()` pop-out as disproportionate risk for the same benefit. (3) **AI Suggestion not idempotent**: `page.tsx` hardcoded `loading={false}` on `SuggestionPanel` — `useMapping` never tracked an in-flight suggestion request at all, so repeated clicks during a slow LLM-backed run fanned out duplicate `suggest_mappings_task` executions (each task's own dedup reads an independent DB snapshot). Fixed both sides: a ref-based frontend guard (mirrors the existing `flushingRef` autosave pattern) plus a new additive `Mapping.pending_suggestion_task_id` column so `request_suggestions` returns the in-flight task's id instead of enqueuing a second one, cleared unconditionally in the task's `finally`. **Live-verified against the real broker**: two rapid requests returned the same task_id; the real task ran 298.8s (~5 min, real Ollama calls, one retried after a timeout) before clearing — direct confirmation of the user's "takes longer" report and that the guard holds at real-world timing, not just mocked. Applied the additive `ALTER TABLE mappings ADD COLUMN pending_suggestion_task_id VARCHAR` to live `dataone-postgres` and rebuilt+restarted `api`/`worker`/`beat`/`frontend`. (4) **Manual target-column click did nothing**: the click-to-map flow was source-first only (stage source(s), then click target) with zero feedback if a target was clicked with nothing staged — looked like a dead button. Now every unmapped target responds to a click: connects if sources are staged, otherwise shows a guided hint via a new `hint()` hook action (reuses the existing toast, not a new mechanism) plus a real tooltip/focus ring. Deliberately did not add a symmetric target-first flow (bigger change to an already multi-round-tuned component). Verified: backend 1014/1014 (6 new tests across `test_mapping_service.py`/`test_suggest_task.py`), frontend `tsc`/ESLint clean, vitest 99/99 for touched suites (full-repo vitest has 7 pre-existing unrelated `QueryWorkspaceInner`/`useRouter`-mock failures, confirmed via `git stash` to predate this session), production build clean (25/25 routes). No browser-automation tooling available, so #1/#2/#4's visual/interaction feel is untested beyond RTL + live API; #3 has real end-to-end evidence past that layer.

- 2026-07-22 — Audited the frontend against `E2E_DEMO_WALKTHROUGH.md` (Scenario A, CRM→DW), findings in `e22_observations.md`. Fixed the one real functional gap and one UX gap it found, plus label mismatches. **Pipelines**: added `Pipeline.execution_mode` ("auto" runs once immediately on create, reusing the same run-trigger path as "Run now"; "manual" is today's behavior) and `Pipeline.load_strategy` ("upsert"/"full_refresh"/"append" override the previous implicit natural-key inference in `pipeline_executor.py`'s `_batch_copy`; `None` preserves old behavior exactly for pre-existing pipelines) — both additive nullable/defaulted columns via the existing `main.py` startup `ALTER TABLE` pattern (no Alembic in this repo). Added a real `append` load path (`_append_batch`, insert-only, never delete/upsert) since it didn't exist before. **Visualize**: added a "Visualize this result" action in Query Workspace that hands off the current result page (sessionStorage, mirroring the existing `query-workspace/lib/handoff.ts` pattern) to a new Visualize "query mode" — dimensions/measures are aggregated client-side (`visualize/lib/clientAggregate.ts`) since ad hoc SQL results have no server-side GROUP BY to run; `ChartCanvas` needed zero changes since it already consumed a generic `{columns, rows}` shape. Saved views stay catalog-only (`VizView` has no raw-SQL/rows column). **Labels**: "Scan catalog"→"Scan Schema", "Get AI Suggestions"→"AI Suggest", and two transformation kind labels got a parenthetical alias (`Coalesce`→"Conditional (Coalesce)", `Cast`→"Format (Cast)") — cosmetic only, no grammar/backend change, since none of the 11 existing kinds do real threshold IF/THEN logic. Added 7 new backend tests for the load_strategy/execution_mode branches. Verified: backend 1008/1008, frontend lint clean on every touched file (pre-existing unrelated errors only in `tenants/`). **Not yet done**: an actual rebuilt-container browser click-through (the running `dataone-*` containers predate these changes and have no source volume mount) — offered to the user, not yet performed.

- 2026-07-22 — User tested the just-completed UI/UX reuse pass (`tasks.md` #03–#09) and reported 4 real problems (Topology layout overlap w/ screenshot, Data Visualization controls unresponsive, Schema Mapper icon/notification overlap, Query Workspace losing conversation on tab-away-and-back). Filed as `uiux_reuse_bugs.md` (repo root, tracked in git unlike `requirements-specs/*`) and fixed 6 of 7: the shared `.workspace-header` (`globals.css`, used by all 14 migrated pages) had no `flex-wrap`, so a wide actions row crushed the title into a one-word-per-line stack — exactly what task #12 (cross-module verification, not yet started) exists to catch; fixed at the shared-component level. Also moved Topology's node search out of a canvas overlay that structurally competed with target-node placement, fixed 5 separate `Toast` components rendering on top of the sticky shell header (`top-4` sits inside the header's 64px band, `z-50` outranks it), hardened Schema Mapper's own header rows the same way, fixed `ChartTypeSelector` disabling its own default/active selection, and fixed AskData's conversation being silently discarded on route navigation (was pure component-local state; now persists `session_id` to `sessionStorage` and rehydrates from the existing `GET /askdata/sessions/{id}/messages` endpoint, plus a new "New chat" action). One item (vague "no actions work" on Visualize) stayed `[?]` — no additional root cause found beyond what the other fixes cover; needs a screenshot/repro. Verified: full frontend 286/286, `tsc --noEmit` clean, ESLint clean on every touched file (repo baseline unchanged), production build clean (25/25 routes). **Uncommitted.**

- 2026-07-22 — Closed UI/UX reuse #09 (Governance + Risks + Security + Audit). Standardized shared headers, glass filters/tables, semantic severity/outcome states, responsive table/detail/editor layouts, Security segmented navigation, confirmation dialogs, Audit details/correlation/pagination/export. Security privileged confirmation now reuses `DialogSurface`; all authorization, role/permission, masking, row-filter, governance authoring, risk aggregation, and append-only audit API behavior remains unchanged. Added Security's first 2 page tests. Focused 21/21, full frontend 286/286, TypeScript/module ESLint/diff check, production build clean.

- 2026-07-22 — Closed UI/UX reuse #08 (Data Quality + Semantic/Metrics). Data Quality now uses shared header/connection context, glass score summary/table scorecards, and responsive wide score tables, with accuracy/freshness still honestly unavailable. Semantic now has shared header, responsive catalog/detail workspace, semantic draft/published/certified states, shared metric loading/error surfaces, glass definition/editor cards, and consistent actions; all role, draft/publish, definition, and lineage APIs unchanged. Added Semantic's first 2 frontend tests and removed its stale unused `useMemo` import. Focused 7/7, full frontend 284/284, TypeScript/module ESLint/diff check, production build clean.

- 2026-07-22 — Closed UI/UX reuse #07 (Topology + Visualization + Impact). Added shared headers and responsive config/graph/inspector layouts, standardized topology connection/view/search/legend/summary/annotation/detail surfaces, and migrated all hardcoded topology/Recharts colors (series, axes, grid, tooltip, edge, risk, state) to CSS semantic variables. Impact selectors and dependency/risk cards now use shared surfaces. No behavior changes to graph layout/search/clustering/LOD/merge/view, live viz query/export/saved-view flows, or impact traversal. Focused 34/34, full frontend 282/282, TypeScript/module-wide ESLint/diff check, production build clean.

- 2026-07-22 — Closed UI/UX reuse #06 (Schema Mapper). Completed the semantic-token migration across Canvas, Properties, Validation, Suggestions, Transform, Publish, Export, Comments, History, review stages, heatmap, toast, and reporting, mapping legacy palette utilities to the platform tokens while retaining meaningful source/target, transformation, business-rule, and status distinctions. Fixed the existing `ExportModal` `setState`-inside-effect lint error using the repo's render-time prop-change reset pattern; copy/download unchanged. Mapper 63/63, full frontend 282/282, mapper-wide ESLint, TypeScript, diff check, and production build clean.

- 2026-07-22 — Started UI/UX reuse #06 (Schema Mapper) after closing Connectors. Migrated the outer page, selected/loading states, mapping rail, mapping toolbar, and autosave/version context to shared workspace/semantic tokens; preserved all mapper APIs/state, drag-drop Canvas, review/version flows, and existing E11 docked Panels. Did not mark complete: nested Canvas/Properties/Validation/Suggestions/Transform/Publish/Export/Comments/History still contain legacy local color utilities and need a separate controlled pass. Mapper 63/63, full frontend 282/282, TypeScript/focused ESLint/diff check, and production build clean.

- 2026-07-22 — Closed UI/UX reuse `tasks.md` #03 (Connectors). Added shared `DialogSurface` (labeled modal semantics, Escape/backdrop/explicit close, responsive overflow, optional footer/width) and migrated the remaining edit, rotate-credentials, dependency-aware delete, and activity overlays. Also fixed ConnectorAuditLog's previously hidden fetch failure: it already held unused error state and collapsed failures into a misleading empty state; it now shows the real API error while preserving console diagnostics. Existing API payloads, credential test-before-save option, soft-delete dependency inspection, and audit filter are unchanged. Added 2 dialog tests; full frontend 282/282, TypeScript/focused ESLint, diff check, and production build clean.

- 2026-07-22 — Continued the UI/UX reuse build. Closed `tasks.md` #02 (shared foundation), #04 (Query Workspace), and #05 (Schema Intel + Schema Comparison). Query Workspace now carries the shared semantic styling through AskData and SQL subviews, labeled connection controls, accessible History/Saved tabs, responsive sidebar, semantic alerts/actions, while preserving mounted mode state, handoffs, and write confirmation. Schema Intel/Comparison gained shared headers, glass tool/filter surfaces, labeled selectors/search, semantic actions, and shared async/empty states with all existing APIs/roles intact. Connectors advanced through reusable Card surfaces, accessible action menu, semantic actions, and improved create/schema dialogs; #03 stays `[~]` because edit/rotate/delete/activity overlays remain. Verified frontend 280/280, TypeScript + focused ESLint clean, `git diff --check` clean, production webpack build clean.

- 2026-07-22 — Began the cross-module UI/UX reuse pass requested from `Schema Designer.zip`. Audited the archive and explicitly excluded `.lovable`, Supabase auth/RLS/migrations/types, TanStack Start/router/server functions, generated routing, Lovable AI/error reporting, and duplicate archive data models. Added the Data One-specific translation at `prompts-v2/uiux-prompt.md/uiux-update-build.md` and the priority backlog at root `tasks.md`. Built the first reusable frontend slice: `WorkspaceHeader` + generic `SegmentedControl`, semantic workspace/accent CSS, and migrated Connectors plus Query Workspace chrome/states to those primitives without changing their real API, handoff, mounted-mode, or write-confirm behavior. Added 2 component tests; full frontend 280/280, focused ESLint and TypeScript clean, production webpack build clean (required network access for existing `next/font` Geist downloads). Remaining tabs are sequenced in `tasks.md`; tasks 02–04 remain `[~]` because this is the first slice, not a claim that every sub-surface is migrated.

- 2026-07-22 — User pointed at `prompts-v2/uiux-prompt.md/uiux-prompt-schemamapper.md` (a generic, stack-agnostic UI/UX brief — names Lovable Cloud/Supabase/TanStack Start, none of which exist here) and asked to build schema mapper functionality against it. Full feature-by-feature comparison written to `requirements-specs/enterprise_v2/schema_mapper_uiux_notes.md`: almost everything the spec asks for already exists and is more advanced than the spec describes (versioned drafts, drag-drop canvas, AI suggestions with confidence breakdown, validate/publish, comments, review workflow, documentation/migration reports). The one genuine, well-scoped, non-decision-gated gap was the spec's `/mappings/$id/history` route and `createDraftVersion` function — closed as `E13-5`/`E13-6` in `requirements-specs/enterprise_v2/E13_mapping_review_workflow.md`. Grounding finding: there was previously **no real way to reopen a published mapping for editing** — the earlier "→ Draft" review-stage button only flipped the `review_stage` label, never the `status` column `publish()`'s `_assert_draft` guard actually checks; the only prior "reopen" mechanism was a raw `m.status = "draft"` bypass inside two unit tests. Built `MappingService.create_draft_revision` (clones an old version's `edges_snapshot` into fresh draft edges; serves both "Revise" and "Rollback" through one mechanism, never mutates `mapping_versions` history), `list_versions`/`diff_versions`, 3 new endpoints, a `VersionHistoryPanel` (docked panel, not a new route — matches this app's existing single-page mapper architecture), a `WorkspaceHeader` "↺ Revise" button, and a review-stage badge on the mapper list. **Found and fixed a real latent bug while building this**: `publish()` iterated `m.edges` unfiltered (harmless when a mapping could only ever publish once; corrupts version history and can crash on a UNIQUE constraint once a second edit generation exists) — same bug existed in `MappingValidationService.validate_mapping`, fixed the same way (filter to `version_id is None`). Remaining open gaps documented, not built this pass: SQL/dbt export formats, a DDL/CSV "schema library" (scoping decision), a dedicated review-queue frontend page, transition notifications. Verified: backend 1001/1001 (15 new/updated tests), frontend 278/278 (21 new), `tsc --noEmit`/ESLint/`next build` all clean. **Uncommitted.**

- 2026-07-20 — Resolved the E05–E08 validation report: fixed 6 grounded issues (shared-filter risk cache, DQ unknown-row correctness + constant-query summary, per-connection governance score UI/API, constant-query impact mapping traversal, eager risk-validation edges) and reclassified 4 drift/scoring/security/retention observations as documented product semantics. Backend 946/946, frontend 247/247, focused lint and production build clean; concurrent mapping/report work preserved.

- 2026-07-19 — Fixed all 12 deduplicated valid E01–E04 findings in `enterprise_v2/build_validation_bugs_v3.md`: corrected published/active-only KPIs + Total Schemas, restored dashboard polling recovery, hardened Copilot focus and per-connection sessions, removed fake value-pattern evidence, corrected topology placeholder/renamed-lineage/all-unmatched-pair behavior, rejected deleted graph connections, and grounded/revalidated AI suggestions. Backend 933/933, frontend 235/235, focused lint and production build clean. Preserved concurrent E13/report work.

- 2026-07-19 — Revalidated both Enterprise v2 E01–E04 build-bug reports and added `build_validation_bugs_v3.md` as the authoritative consolidated backlog. Kept 7 original findings after deduplication, retracted/reclassified unsupported claims, and added 5 grounded defects: deleted connector leakage into KPIs/topology, first-pair-only topology AI evidence, ungrounded AI source suggestions, and cross-connection Copilot session context. Documentation only; unrelated in-flight E13/schema-mapper changes were left untouched.

- 2026-07-17 — Added a deterministic `E2E_Retail_Warehouse` SQLite target paired with `E2E_Retail_Analytics`, auto-registered independently on startup so existing installations receive it. Its empty warehouse schema covers semantic AI matching, PK upsert, >1-batch fact loads, nullable/PII/orphan data, no-PK full replacement, null-safety validation, unsupported-transform failure, and drift blocking without claiming non-direct pipeline support. Added seed/idempotency tests and an executable scenario matrix to `docs/E2E_TESTING_GUIDE.md`. Focused seed tests 2/2 and Python compile pass; wider pipeline tests 5/6 with one pre-existing notification fixture failure (`in_app_notifications` table absent poisons the session after notify persistence fails).

- 2026-07-17 — **Topology + Query Workspace live corruption fix.** Follow-up runtime audit found E02 still had two live defects after B01-B14: graph reads synchronously waited on Ollama retries (9-25s observed) and multiple AI column matches drew duplicate table-pair edges/labels. Fixed with `AIService.match_schemas(..., allow_llm=False)` for topology graph reads and one aggregated suggestion edge carrying column evidence; rebuilt Docker API/frontend smoke now returns `/schema/graph` in 0.02s with no duplicate same-pair edge types. Also repaired Query Workspace consolidation drift: shell/header/dashboard now route to `/dashboard/query-workspace` modes, old AskData/Query Studio pages are redirect-only, query dashboard links target `?mode=sql`, connector-load failures show retry instead of silently disabling the workspace, and UI copy says “Edit in SQL.” Verified backend focused 119/119, frontend focused 52/52, touched lint clean, production build clean, rebuilt-stack live smoke passed.

- 2026-07-19 — **Enterprise v2 program: Wave 1 (E01–E09) all shipped progress; Wave 2 underway (E12/E10/E13 done).** Full detail lives only in `requirements-specs/enterprise_v2/INDEX.md` + each epic's own progress log; this is a pointer, not a duplicate. Wave 1: E01/E02 essentially done except tasks hard-gated on E08/E09/tenant-isolation; E03/E04 have real slices; E05 Risk & Compliance Center, E06 Data Quality Observatory, E07 Impact Analysis (v1 live traversal, not the blocked E02-9 store), E08 Governance Intelligence (`GovernanceMetadata` replacing deleted hardcoded `dama_metadata`), E09 Unified Copilot (found `dba_intent_classifier` already unified askdata/agentic-dba/ACI; added a 4th `platform_insight` intent + persistent `CopilotPanel`) — each shipped a real backend service + API + full frontend page + KPI feed. Wave 2: **E12** Schema Comparison Mode (extends `DiffService`, adds constraint diffing); **E10** Schema Mapping Workbench — scoped to E10-7 Transformation Preview only, since the topology file was a recurring collision site with a parallel session all pass (table-card progressive disclosure E10-1/2/3/6/8/9/10/11 deferred until it's not contested); **E13** Mapping Review Workflow — added `Mapping.review_stage`, deliberately independent of `status` (which already gates ~10 real call sites on "is this mapping live"). Recurring theme: every new KPI/score models "no data yet" as `status="loaded"` + honest zero (Migration Readiness precedent), never `"unavailable"` or fabricated. E02-9 (persisted lineage store) remains the one hard infra blocker — tenant isolation, not a skippable decision. Backend suite 921/921, frontend suite 228/228 as of the last verification pass.

- 2026-07-16 — Added `DATAONE_DEMO_SCRIPT.md`, a presenter-ready high-level walkthrough of the full DataOne lifecycle: dashboard, connectors, schema intelligence/topology, semantic metrics, mapping, pipelines, Query Workspace, visualization, AI Autopilot, security, integrations, and audit. Includes a 15–20 minute main flow, 5-minute executive cut, talk track, value statements, preparation notes, and safety/tenant-readiness guardrails. Linked it from the README demo section.

- 2026-07-16 — Added `backend/run_integrations_demo.py`, a high-level presenter walkthrough of all current ACI integration capabilities. Default mode is network-free; optional `--live` mode performs authenticated read-only checks of status, governed actions, linked accounts, and admin notification policies on port 8011. The demo deliberately cannot mutate settings or execute external functions. Documented usage in `integrations_tab.md`.

- 2026-07-16 — Added root `integrations_tab.md`, grounded in the shipped ACI integration and current official ACI SDK/docs. It documents actors, current UI/API/client boundaries, five use cases, outage behavior, configuration, multi-tenant owner-ID requirements, and the direct-call decision: DataOne backend may call ACI app/function/linked-account details through `AciClientService`; browser-direct calls are prohibited because they expose `ACI_API_KEY` and bypass governance/audit/tenant scoping. Proposed read-only proxy endpoints; explicitly rejected a generic execute-any-tool endpoint.

- 2026-07-15 — Fixed oversized Veltris artwork across landing/login/dashboard. The shared `Brand` component used `w-auto h-auto`, overriding Next Image's numeric dimensions and exposing the PNG's 3930px intrinsic width. Brand chrome is now shrink-proof and bounded to 94×16px standard / 76×13px compact inside a 28px-high mark container; added component regressions for both sizes.

- 2026-07-15 — Diagnosed transient `dataone-api` unhealthy startup after fresh Docker initialization. Both Gunicorn workers ran `Base.create_all`, admin/RBAC seeds, and shared SQLite demo seeds concurrently, causing PostgreSQL sequence/user uniqueness collisions and partial SQLite writes; restart eventually recovered by luck. Serialized initialization with a PostgreSQL session advisory lock across workers/replicas. Also fixed two fresh-seed defects exposed by the race: a function-local `import random` made an earlier `random.Random` reference unbound, and finance invoice date generation passed a string to `randint`.

- 2026-07-15 — Fixed Docker BuildKit collision after the DataOne image rename: `api`, `worker`, and `beat` all declared the same `dataone-backend:latest` build, so parallel Compose exports failed with “image already exists.” `api` is now the sole backend image builder; worker/beat consume the shared image without separate build declarations.

- 2026-07-15 — Renamed the Docker runtime namespace from `dataplane` to `dataone`: Compose project, all custom containers, custom backend/frontend/ACI images, default network, persistent volumes, PostgreSQL application database, Celery app identifier, ACI linked-account owner default, and `DATAONE_BASE_URL`. Updated Docker/E2E documentation and backend user-facing product strings. Third-party image names and internal service ports remain unchanged. Docker daemon inspection confirmed there were no existing DataOne/dataPlane containers, images, or volumes, so no local volume migration was needed; legacy seeded emails and persisted demo filenames intentionally retain `dataplane` for compatibility. Verified full-profile Compose config; affected backend suites 236 passed before one expected owner-name assertion was updated, then ACI 38/38 and frontend integration 5/5 passed.

- 2026-07-15 — Moved DataOne's host-facing ports to frontend `3011` and API `8011`; internal nginx/FastAPI ports remain `3000`/`8000` for service networking and health checks. Compose now passes `NEXT_PUBLIC_API_URL=http://localhost:8011` as a frontend build arg so the static export does not retain the old API origin. Updated `.env.example`, frontend fallback, notification-link base URL, READMEs, and the mapper smoke-test instruction. Audited host ports: `3011`, `8011`, optional ACI `8010`, and ACI portal `3001` were free; Postgres/Redis remain unexposed and therefore do not conflict on the host.

- 2026-07-15 — User explicitly authorized launching application-wide tenant-isolation implementation/review, overriding Task #7's prior product-decision block. Marked v1.1 #7 `[~]` and grounded the plan against the repository: no backend tenant/membership model or tenant routes exist; only nullable string `pipelines.tenant_id` exists; frontend tenant components currently target nonexistent APIs. Plan uses UUID-string tenant IDs, explicit backend `TenantContext`, fail-closed membership authorization, a `legacy-veltris` backfill, versioned migrations before constraints (create_all cannot alter live tables), six waves covering roots/children, APIs, workers/caches/audit/secrets, frontend switching, and adversarial cutover review. This authorization does not waive SSO/Marketplace release gates.

- 2026-07-15 — Extended DataOne v1.1 specs with a security-gated dual-identity/Marketplace architecture (`requirements-specs/v1.1/06`–`12`). Decision: Veltris publisher access uses a separate single-tenant Entra lane with exact Veltris `tid` + immutable identity + publisher app role; subscriber access uses a publisher-verified multitenant Entra lane mapped by `(tid, oid)` to an active, tenant-isolated membership and Marketplace entitlement. FastAPI remains the only application auth authority; root Express `auth.ts` is treated as a prototype, not wired in. Added SaaS Fulfillment API v2 landing/resolve/activate/webhook/reconcile lifecycle, optional SCIM, and explicit security/legal/Partner Center release gates. No SSO runtime code landed because app-wide tenant isolation is not implemented; tasks #7–#10/#12 are `[!]`, SCIM #11 is `[?]`.

- 2026-07-15 — Built the DataOne v1.1 brand/auth/dashboard pass (`requirements-specs/v1.1/`). Frontend user-facing `dataPlane` branding is now DataOne; the supplied Veltris logo is served locally through a shared `Brand` component on landing/login/dashboard. Moved the empty root `auth.ts` intent into `frontend/src/lib/auth.ts`: login now uses the shared API chokepoint, dashboard content waits for `/auth/me` validation, identity and live connector count replace static admin/5-DB claims, and logout is centralized. Dashboard shows summary freshness and current Query Workspace/Connections actions. Verified Vitest 129/129, tsc clean, changed-file lint with 0 errors (3 existing image warnings), production build clean. Full lint still has 6 pre-existing effect-rule errors outside this scope. Auth task remains `[!]` pending human production security sign-off; no refresh-token/SSO/cookie/backend-policy change was attempted.

- 2026-07-15 — Validated the completed v3 Agentic DBA, v4 ACI, and v5 Keeper secrets
  implementations and added per-epic `bugs.md`/`enhancements.md` reports. Fixed two v5 issues:
  `tests/secrets/__init__.py` shadowed the stdlib `secrets` module and aborted pytest collection on
  Python 3.13; partial credential rotation swallowed vault read failures and could overwrite a
  multi-field record with only the submitted field. Rotation now fails closed with a regression
  test. Targeted suites: v3 81, v4 50, v5 41; frontend Vitest 125; TypeScript and production build
  clean. Final post-fix backend suite: 797/797.

- 2026-07-14 — Scoped (spec only, no code) a new epic — **ACI.dev External Tools Integration**
  (`requirements-specs-v4/TRD_DataPlane_ACI_External_Tools_Integration.md` +
  `aci_integration_tasks/INDEX.md`, 11 tasks). Origin: capability-brainstorm on
  [ACI.dev](https://github.com/aipotheosis-labs/aci) (Apache 2.0, self-hostable — 600+ external
  SaaS tool integrations, multi-tenant OAuth/secrets, dynamic tool discovery via search+execute
  meta-functions). dataPlane today has zero path from any module to an external (non-database)
  system — this adds one, narrowly: a `aci_client_service.py` wrapper (reusing the existing
  `CircuitBreaker` class, same pattern as Ollama), a new external-side-effect governance category
  extending `autopilot_registry.py`'s existing allow-list (default-deny, same stance as
  `ddl_execute`), notify-out fan-out for Autopilot/Agentic DBA Copilot approval queues and pipeline
  failures, and a new `"external_action"` AskData intent. Deliberately deferred:
  bidirectional "approve from Slack" (Task #6, `[?]` — needs real inbound-webhook signature
  verification + external-identity-to-dataPlane-role mapping design, not a fast-follow). Task #10
  is `[!]` blocked pending tenant isolation — **eighth** epic to hit that same deferred gap. Not
  started.
- 2026-07-14 — Scoped (spec only, no code) a new epic — **Agentic DBA Copilot**
  (`requirements-specs-v3/TRD_DataPlane_Agentic_DBA_Copilot.md` +
  `agentic_dba_tasks/INDEX.md`, 12 tasks) — triggered by a real user report: asking Query
  Workspace's Ask mode a complex build request ("create new target schemas for retail analytics
  in postgresql based on profiling... data quality steps, transformations and final target
  tables") returned a meaningless `SELECT * FROM tables[0] LIMIT 50` query. Root-caused via code
  audit: `NL2SQLService.generate_sql` has zero intent classification and that heuristic fallback
  fires whenever no read-query keyword matches — not a bug, a scope boundary (AskData is
  deliberately SELECT-only, refuses non-SELECT output). Designed as a **second capability**,
  reusing rather than duplicating existing infra: Autopilot's governance/allow-list pattern
  (`autopilot_registry.py` already hard-bans `ddl_execute` from auto-execution — new
  `schema_design_create` action follows the same `auto_capable=False` precedent, not a new
  authorization model), Schema Mapper's transformation grammar + draft/validate/publish lifecycle
  (reused unmodified for generated mappings), Query Studio's existing DDL-capable write-execution
  path (reused for actual table creation, no new executor), and the legacy
  extract→match→diff→migrate flow in `ai_tasks.py`/`schema_mapper_service.py` (closest prior art
  for a chained, human-gated plan). Core non-negotiable: plan → approve → execute, never
  autonomous, under any policy setting — mirrors this repo's own existing stance on `ddl_execute`.
  Task #11 is `[!]` blocked pending the tenant-isolation architecture decision
  (`requirements-specs/tenant_isolation_tasks/`) — this is the **seventh** epic to hit that same
  deferred gap (mapper, schema intel, connectors, dashboard, autopilot, the original schema-mapper
  review, now this one). Not started.
- 2026-07-14 — Scoped (spec only, no code) a new epic to merge AskData Bot and Query Studio —
  both already fully built as separate sidebar tabs (8/9 FRs each) — into one dashboard tab with
  an Ask/SQL mode toggle. `requirements-specs-v2/query_workspace_tasks/INDEX.md` + 8 task files
  (shared shell keeping both subviews mounted, single connection source of truth, replacing the
  existing `sessionStorage["qs-handoff"]` + hard-navigation link between them with an in-shell
  callback, write-confirm-modal-survives-mode-switch guardrail, keyboard-shortcut scoping,
  sidebar/route consolidation with redirects for the old `/dashboard/askdata` and
  `/dashboard/query-studio` paths, an audit-trail label-clarity check, and test consolidation).
  Extended same day per follow-up: added tasks #9-#12 for "Investigate in Query Workspace"
  handoffs from Schema Intel (drift events, PII/classification badges) and Schema Mapper (AI
  suggestions, validation issues) — a new, generalized `sessionStorage`-based `WorkspaceHandoff`
  contract (distinct from the #1-#8 in-shell callback, since Schema Intel/Mapper stay separate
  routes). Confirmed via audit: neither feature has any existing outbound cross-tab link today,
  and neither TRD specifies this as an FR — net-new UX. 12 tasks total, not started.
- 2026-07-14 (same day, later) — Query Workspace epic built (by a separate session) and then
  reviewed + fixed in this pass — full detail in
  `requirements-specs-v2/query_workspace_tasks/INDEX.md`'s progress log,
  `bugs.md`/`enhancements.md` in that same directory. New: `/dashboard/query-workspace`
  (`QueryWorkspaceInner.tsx` + `AskDataView`/`SqlWorkspaceView` subviews, both kept permanently
  mounted and toggled via CSS so state survives a mode switch), `handoff.ts`'s
  `WorkspaceHandoff`/`writeWorkspaceHandoff`/`readAndClearWorkspaceHandoff`, "Investigate →"
  actions wired into Schema Intel (`DriftHistoryPanel`, `CatalogTableCard`) and Schema Mapper
  (`SuggestionPanel`, `ValidationPanel`), old `/dashboard/askdata` + `/dashboard/query-studio`
  routes replaced with thin redirects. Review found and fixed 5 real bugs before calling it done:
  a hardcoded placeholder query (`SELECT * FROM related_table`) on one Investigate action — a
  genuine no-mock-UI violation, not just a style nit; a half-wired background-completion badge
  that could never fire for AskData (SQL side worked, Ask side didn't); dead
  `sessionStorage["qs-handoff"]` + hard-navigation code left in `ChatBubble.tsx` that task #3 had
  explicitly said to delete; a shipped SQL-handoff mechanism (ref + `setTimeout(...,0)`) that
  wasn't the one its own tests exercised; and, caught only by running `npm run lint` (not manual
  review) — this repo's ESLint config already enforces the newer `react-hooks/refs` /
  `react-hooks/set-state-in-effect` rules — a shell-level modal reading `ref.current` during
  render and a mount-only effect doing 5 setState calls instead of using a lazy `useState`
  initializer. **Lesson worth repeating for future epics: always run the project's actual lint
  config as part of "done," not just tsc/build/tests** — it caught two real bugs neither manual
  review nor the test suite would have. Also added the test coverage the epic's own tasks #8/#12
  asked for but hadn't landed (0 → 106 vitest tests passing for this epic's code, up from having
  none for the shell itself or any of the four investigate actions). Verified: `tsc --noEmit`
  clean, `npm run lint` clean for every file touched, `npm run build` clean (19/19 routes),
  `npx vitest run` 106/106. Not yet exercised against a live backend/browser — no Docker/browser
  automation available this pass. **Uncommitted.**
- 2026-07-09 — Resolved the decision-blocked items surfaced by the bug-validation sweep, per user choice ("just resolve the decisions, implement what's unblocked"): (1) **Mapper #02 keyboard a11y** — `Canvas.tsx`'s existing click-to-stage/click-to-connect flow (built for N:1 mapping) is now keyboard-operable: actionable rows get `tabIndex=0`/`role=button`/`aria-pressed`/`aria-label`, Enter/Space fire the same `activate()` as click, plus a visually-hidden `aria-live` region announcing staging/connection events. Reused the existing mechanism rather than inventing a new interaction model. (2) **Mapper #04 virtualization** — added `react-window` (`FixedSizeList`), which forced a real architectural fix alongside it: the connector-line SVG overlay assumed absolute unscrolled row positions, so virtualizing without more would have silently misaligned connector lines the moment a panel scrolled. Fixed by tracking each panel's scrollTop and having the overlay hide (never mis-draw) a connector whose endpoint has scrolled out of view. Below the new bounded-height cap (560px), rendering is pixel-identical to before — only large schemas actually virtualize. 7 new tests in `schema-mapper/__tests__/Canvas.test.tsx`. (3) **Schema Intel #05 classification confidence** — presented 3 options, user chose defer entirely (real confidence needs profiling/FR2 first; a rule-based interim score would be a second path to retire later — not worth it). Documented, no code. (4) **Tenant isolation** — user chose "draft the architecture decision + epic spec, no code." Wrote `requirements-specs/tenant_isolation_tasks/00_architecture_decision.md`: recommends row-level `tenant_id` + Postgres RLS (not app-filtering alone — a missed WHERE clause is a real leak) over schema-per-tenant or DB-per-tenant, full 11-root/13-child table inventory, data model + auth sketch, 5 open questions for Security/Product (tenant cardinality per user, physical-isolation requirement, platform-superadmin bypass, rollout sequencing, legacy-data backfill). This is the **first substantive proposal** after six prior epics (mapper #7, schema_intel #9, connector #10, dashboard #9, autopilot #11, plus the original `review_schema_mapper_tasks/CONTRADICTIONS.md` C4) all independently flagged the same gap and said "wait for a decision" — updated all six to point here instead of restating the finding a seventh time. Also noted: `Pipeline.tenant_id` (a nullable `String` placeholder added speculatively during the pipelines build) needs retyping to a real FK once this lands. Verified: frontend tsc/build clean, vitest 70/70 (17 new), lint at 29-problem baseline; backend untouched this round (docs + frontend only). Live: frontend container rebuilt/deployed; no browser-automation tooling (Playwright/chromium-cli) available in this environment, so the interactive keyboard/virtualization behavior was verified via RTL's real-DOM/real-event simulation rather than an actual browser screenshot — flagged explicitly rather than overclaimed. **Uncommitted.**
- 2026-07-09 — Dashboard bugs round 2: fixed #02, #03, #05 from `dashboard_tasks/bugs/INDEX.md`; reviewed #01 and left it by design (see that INDEX's disposition note — accepted per-process cache tradeoff, not a defect); #04 already confirmed stale/fixed in the round-1 pass. Frontend-only changes: `useWidgetData` fetchers now take `(signal: AbortSignal)` and the hook actually cancels a superseded/unmounted request instead of just discarding its result (`api.get` gained an optional `{ signal }` param); `setUnauthorizedHandler`'s single nullable slot replaced with `addUnauthorizedHandler(fn) -> unregister` backed by a `Set`, so a second concurrent registrant can't clobber the first (only caller, `useMapping.ts`, migrated — no shim kept); new `page.test.tsx` (9 tests) closes the real gap in #05 — every dashboard leaf component already had tests, `page.tsx`'s composition/wiring logic (237 lines) didn't. New `src/lib/__tests__/api.test.ts`. Verified: tsc clean, vitest 63/63 (16 new), lint at 29-problem baseline, build clean. Backend untouched this round. **Uncommitted.**
- 2026-07-09 — Time-boxed review pass across the newly-added `requirements-specs/*/bugs/` validation reports (autopilot, connectors, dashboard, mapper, pipelines, schema_intel — one per epic). Fixed the one live, actionable, previously-under-scoped item found: connector_tasks bug #03 (SQL injection via f-string table-name interpolation) was filed against Postgres only, but the same pattern existed in `mysql.py` (pointless f-string, no interpolated value) and `sqlite.py`/`oracle.py` (`PRAGMA table_info({table_name})` / `PRAGMA foreign_key_list({table_name})`, which can't take bound params — fixed via identifier quoting instead). All 4 connectors fixed; suite re-run 335/335 (no new test — no reachable malicious-input path since table names only ever come from `get_tables()`, fixed as defense-in-depth per the bug's own "high in principle" framing). Also found dashboard_tasks bug #04 ("per-module rollback missing") to be a **stale report** — the rollback was already added in the 2026-07-07 build; corrected the record rather than re-fixing already-fixed code. Left untouched, correctly: pipelines bugs #12–19 (already fixed pre-existing), mapper #02/#04 ([?] need human input) and #07 ([!] blocked), schema_intel #04/#06/#07/#08 (unbuilt features, not bugs) and #05 ([?]), dashboard #01/#02/#03/#05 (LOW, legitimate backlog, not broken behavior) — none of these are "fix in 10 minutes" scope.
- 2026-07-09 — Autopilot bug round: 5 validated findings in `requirements-specs/ai_autopilot_tasks/bugs/`, all fixed + 6 regression tests (suite 335/335, autopilot 71/71). Fixes: dispatch-after-commit pattern for the migration executor (executors NEVER commit the caller's session — reserved `DISPATCH_AFTER_COMMIT_KEY` callable fired after the owning commit); engine sweep skips+counts invalid drafts instead of dying; refresh/expire after every bulk `.update(synchronize_session=False)`; newest-drift-per-connection via SQL `group_by`+`max(id)` instead of loading the window into Python; structured `blocked_by` key in block details/audit payloads. **`bugs/notes.md` is the reusable lessons doc — it ends with a pre-flight checklist to copy into the next epic's INDEX.** Third consecutive epic where post-completion review found real bugs at 100% test-pass — treat post-epic review as a standing phase. Known accepted deviations documented in notes.md (request_suggestions' internal commit; no auto-recovery for crash-stranded `executing` recs). Images rebuilt + live smoke passed.
- 2026-07-11 — Audit Trail epic (`requirements-specs/audit_trail_tasks/`) tasks #1–#6 done in one day, full detail in that INDEX's progress log. Started from a prior uncommitted session's commit (`34fb9e0`) that had written a lot of code (canonical schema, hash-chain scaffolding, ingestion/search/export routers) but left the **whole backend import-broken** (`AuditLog.metadata = Column(...)` collides with SQLAlchemy's reserved `Base.metadata` — fixed by renaming to `event_metadata`) and completely untested. Testing that code surfaced 3 more real bugs, not just the import break: (1) the hash chain used flush-then-mutate-then-UPDATE, incompatible with a true append-only trigger — refactored to precompute sequence/hash before a single INSERT; (2) hashing `created_at` was flaky because SQLite doesn't preserve tzinfo through a session-expire re-fetch, so `verify_hash_chain` false-flagged every row — dropped timestamp from the hashed content; (3) export's "streaming" loaded the full result set into memory, *and* a naive fix (lazy-query inside the StreamingResponse generator using the request's `Depends(get_db)` session) would have broken silently — FastAPI closes that session as soon as the endpoint returns, before the generator actually iterates — confirmed via a standalone repro before trusting the fix; solved with a dedicated session scoped to the generator's own lifetime. Also added: DB-level append-only trigger (`app/core/audit_guard.py`, Postgres PL/pgSQL + SQLite, installed at startup) verified against real Postgres via `psql UPDATE` → rejected; circuit-breaker + bounded in-process buffer + Celery flush task for ingestion durability (reused the existing `app/core/circuit_breaker.py` pattern from Ollama); composite indexes + a `sort_by` allow-list (unvalidated `getattr` would resolve to `Base.metadata` again and 500); full frontend viewer (`dashboard/audit/` — table/filters/detail/correlation-timeline/export, new `api.download()` in `lib/api.ts`). Verified: backend pytest 442/442, frontend tsc/build/vitest 76/76 clean, lint zero new problems, live smoke against rebuilt `api`/`worker`/`beat`/`frontend` containers (they were crash-looping the whole session on the pre-fix image). **Open:** #7 (retention + role gating — audit router has zero auth today) and #9 (security sign-off) not attempted. **Uncommitted.**
- 2026-07-11 — Query Studio epic (`requirements-specs/query_studio_tasks/`) tasks #1–#6, #8 built end-to-end same day as Audit Trail, full detail in that INDEX's progress log. 8/9 FRs done — only FR7 (send-to-Visualize) is open, and it's a **product decision**, not a build gap: the TRD wants query results sent to Visualize for charting, but `/dashboard/visualize` is actually a ReactFlow schema-relationship graph (tables/PII risk), not a chart surface — same category of TRD-vs-reality mismatch AskData Bot's INDEX already flagged for its own module. New: `query_execution_service.py` (wires in a previously-built-but-unused `statement_classifier.py` — its `sqlparse` import wasn't even in `requirements.txt`, would have crashed in prod on first use), `SavedQuery` model, `/api/v1/query-studio` router (execute/history/saved/export), gated with the existing `require_role("admin"|"analyst")` dependency — first auth on any Query Studio surface. Reads execute through the existing connector `execute_query()` path; **writes go through a separate cursor + explicit `commit()`** — reusing the read path for a write would have silently rolled it back on connection close, since `execute_query()` was only ever exercised read-only before (verified this distinction matters with a real SQLite-file test: re-query after connection close proves the commit actually happened). Write/DDL execution requires admin role **and** explicit `confirm=true`; multi-statement input is rejected outright (stacked-statement write ambiguity). History reuses the Audit Trail infra from earlier this session (`module=query_studio` audit rows) instead of a new table. Frontend: replaced the OLD `/dashboard/query-studio` page, which — per that INDEX's original audit — was actually mislabeled AskData-style NL2SQL chat calling `/api/v1/query/nl2sql`, with a real SQL editor (new deps `@uiw/react-codemirror` + `@codemirror/lang-sql`, schema-aware autocomplete from the existing Schema Intel catalog endpoint, write-confirm modal, history/saved-queries sidebar, CSV export). Verified: backend pytest 458/458 (up from 442), frontend tsc/build clean, lint zero new, vitest 81/81 (up from 76), live curl smoke against the real containers (list-tables SELECT, blocked-then-confirmed write, saved-query CRUD, CSV export all exercised against the seeded SQLite connections). **Test-authoring gotcha worth remembering:** two `client_*` TestClient fixtures used simultaneously in one test both mutate the same global `app.dependency_overrides` dict — the second one silently wins for every request regardless of which client variable calls it, silently defeating "test as two different users" tests. Fixed with a `switch_user()` helper that swaps identity sequentially instead of stacking fixtures. **Also observed (pre-existing, not caused by this change, not fixed):** adding a new table makes one of gunicorn's two boot workers lose a `create_all()` race against the other — harmless (container ends up healthy) but will recur for every future schema addition given this repo's no-migration-tool setup. **Open:** FR7 blocked on product decision; #10 security sign-off not attempted (this is the first surface in the app that lets an authenticated user execute arbitrary write/DDL SQL against a connected database — real review needed before production, not just green tests). **Uncommitted.**
- 2026-07-11 — AskData Bot epic (`requirements-specs/askdata_bot_tasks/`) tasks #1–#5, #7 done same day as Query Studio (which this depends on for its execution path), full detail in that INDEX's progress log. 8/9 FRs done — only FR5's "visualize" sub-part is open, same product-decision block as Query Studio's FR7. Realigned `/dashboard/askdata` from a canned-response "database intelligence" chat into the actual conversational NL-to-SQL bot the TRD wanted: new `askdata_pipeline_service.py` grounds generation in the Schema Intel catalog (falls back to live schema + flags `grounded: false` if unscanned), reuses `NL2SQLService.generate_sql` (shared with legacy `query.py`, not forked) and Query Studio's `statement_classifier` (refuses anything not a plain SELECT — AskData never writes, unlike Query Studio), masks PII columns for the `viewer` role via the existing keyword-based `SecurityService` classifier. Conversation context folds into the question text itself (`generate_sql` has no history param) — documented tradeoff: the fast-path template matcher also sees history-prefixed text, so a prior turn containing an exact template phrase could misfire a later one (judged low-risk). Extended `ChatMessage` with nullable `connection_id`/`sql_text`/`row_count` instead of a new table — another **additive change to an already-populated live table**, so the dev Postgres needed a manual `ALTER TABLE` (same gotcha class the connectors epic hit first). New `POST /askdata/ask` + `GET /askdata/sessions/{id}/messages`; old `/chat`/`/nl2sql`/`/suggestions` left as unreferenced dead code rather than deleted (nothing else calls them, but deleting wasn't asked for). Frontend: real chat UI (connection picker, collapsible SQL per turn, result table, masked-columns notice, "Edit in Query Studio →" handoff writing to `sessionStorage` — Query Studio's page gained the consuming side). Verified: backend pytest 467/467 (up from 458), frontend tsc/build clean, vitest 87/87 (up from 81), and a live smoke test that actually round-tripped through the real Ollama container (`method: "llm"`) — scanned a connection, asked a grounded question, ran a follow-up in-session, created a throwaway viewer-role user and confirmed PII columns came back redacted for them but not for admin, confirmed the audit trail recorded it, then deleted the test user. **Open:** FR5 visualize sub-part (product decision), #9 security sign-off (PII masking here rides on Schema Intel's still-partial classifier — real review needed before this touches anything sensitive). **Uncommitted.**
- 2026-07-08 — AI Autopilot governance epic built end-to-end (`requirements-specs/ai_autopilot_tasks/`, tasks #01–#10; #11 sign-off blocked on human). **Critical hotfix folded in: the legacy autopilot router was fully unauthenticated — including `mode="execute"` which copies data into the target** (same defect class as the connectors hotfix). New: policy/recommendation/action-log models, `autopilot_registry.py` (4-action allow-list + PROHIBITED set + default-deny, auto only if reversible+low-risk, asserted at import), `autopilot_service.py` (guarded-UPDATE status machine; bounded executor: prohibited hard-block → policy/breaker/rate-limit clamps → execute; demote-to-queue fail-safe), `autopilot_engine.py` (drift + connector-health evaluators, deterministic metadata-grounded rationale — deliberately NO LLM on the decision path per TRD §10 prompt-injection risk), `autopilot_tasks.py` (2-min beat + executor task, inline evaluate hooks in drift/health tasks for the ≤10s NFR). Legacy execute-mode now returns `queued_for_approval`. Frontend: autopilot page → 4 tabs (Run console/Approvals/Policy/Action log). Verified: pytest 329/329 (65 new incl. AC1–AC4 safety suite), vitest 46/46 (11 new), tsc/build clean, lint 29 baseline, images rebuilt, full live pass (401 gate, 422 auto-clamp, queue→approve→execute→audit-order, beat firing observed). **Uncommitted.** Gotchas: (1) `AutopilotRecommendation` dedupe_key mirrors the mapper zombie-suggestion lesson — open recs refresh in place, cleared triggers supersede; (2) executor dispatches Celery AFTER committing the rec (worker session can't see uncommitted rows — bit me in the migration executor's FK); (3) circuit breaker computes from the action log at execution time, never mutates policy rows.

- 2026-07-06 — Added `CLAUDE.md`, `SKILLS.md`, `MEMORY.md` at repo root to formalize agentic-SDLC practice on top of `prompts/*.md`, and wrote instructions in a compact, section-scoped style for Kimchi's limited context window. `prompts/*.md` left as-is per decision to treat it as already current.
- 2026-07-06 — Code review of pipelines commit `3866c7e` (Tasks #1+#2) filed and **fixed** bugs #12–#19 (`requirements-specs/Pipelines_tasks/1*.md`). Highlights: the Task #1 refactor had silently destroyed the legacy `POST /execute` executor (restored verbatim + regression tests); the Pipelines INDEX had marked spec-only tasks as done FRs (corrected — real status is 2/10 FRs done); drift check hardened (fail-closed on missing snapshot, column-order-normalized hashing, column-level `changed_tables`, mapping↔connection match enforced at create). Suite: 164/164. Lesson recorded in INDEX legend: `[x]` = landed tested code only, never "spec written". Uncommitted — working tree also holds unrelated staged schema-catalog work.
- 2026-07-06 — User report "schema mapper always shows same tables / no dropdown / static" traced to (a) the **Visualize** tab auto-picking hardcoded connector ids 1/2 with unlabeled selects, and (b) the **dashboard home** being mostly string literals (tiles, activity feed, connection-health %s — only drift alerts were real). Filed + fixed 5 tasks in `requirements-specs/dashboard_static_ui_tasks/INDEX.md`: Visualize now defaults to first-two connectors with labeled 📤/📥 pickers and a proper empty state (was: infinite spinner when <2 connectors); home tiles/feed/health now driven by `GET /connectors/`, `POST /connectors/{id}/test`, `GET /audit/`, `GET /audit/summary`, `GET /mappings/` — unbacked numbers ("Total Tables 15", "AI Matches 92%", "PII 8", health %s) removed per the no-mock-UI rule. tsc/lint/build clean (lint = 30-problem baseline, zero new), frontend image rebuilt + smoke-checked live. **Uncommitted.** Gotcha for next time: seeded demo data uses connector ids 1/2 — magic-id assumptions hide in `query-studio/page.tsx` and `askdata` too (not audited this pass).
- 2026-07-06 — Review round 2 of the schema-mapper epic (same treatment as pipelines): 12 findings filed and **all fixed** (`requirements-specs/review_schema_mapper_round2/INDEX.md` has the full per-bug log). Notable: two HIGHs were regressions introduced by round-1 fixes themselves — the one-at-a-time autosave drain reintroduced a concurrency race the old `splice(0)` was immune to (fixed with a ref-based in-flight guard; state-based guards are useless inside mount-once-effect closures), and the N:1 staging set survived mapping switches. Backend gained `uq_field_target_per_draft` partial unique index (drafts weren't covered by `uq_field_target_per_version` — NULL version_ids are distinct). Verification: pytest 167/167 (3 new), tsc/build clean, lint byte-identical to HEAD baseline. **Uncommitted.** Lesson: post-review fix batches need their own review pass — both HIGHs came from the "fixes", not the original features.
- 2026-07-07 — Dashboard epic built (`requirements-specs/dashboard_tasks/`, tasks #1–#7 + #8-backend; full detail in that INDEX's progress log). New: `GET /api/v1/dashboard/summary?range=` (8 KPI tiles + enriched feed, per-module failure isolation, per-user 30s TTLCache, viewer role masking), frontend `DashboardWidget`/`KPITile`/`ActivityFeed`/`TimeRangeFilter` + `useWidgetData`, `page.tsx` rewired to the aggregation API. New dep `cachetools`. Verified: pytest 191/191 (24 new), lint at 30-problem baseline, api+frontend images rebuilt and endpoint exercised live. **Uncommitted.** Gotchas worth remembering: (1) per-module try/except MUST `db.rollback()` in each handler or one failed query poisons the rest of the session; (2) task-spec pseudocode drifted from real models (`Pipeline` has no `status`, `DBConnection` has no `is_deleted`) — always re-ground specs against `app/models/` before implementing; (3) frontend has NO jest/RTL infra — dashboard spec assumed it exists, component tests need a human decision.
- 2026-07-07 — Dashboard follow-up (user-approved): added the repo's first frontend test infra — Vitest + RTL per the Next-bundled vitest guide (`frontend/vitest.config.mts`, `vitest.setup.ts`, `npm test`); 35 component/hook tests in `src/app/dashboard/__tests__/`, all green; dashboard_tasks #8 now fully [x]. Backend dashboard coverage measured at 90% (pytest-cov in venv only, not pinned). Seeded idempotent demo data via api container (tag: actor/created_by `demo-seed`) so all 8 KPI tiles show range-scoped values — dev Postgres only, delete by that tag. **Uncommitted.** Gotcha: with React 19, `act(() => someAsyncFn())` without await corrupts the act scope and nulls `result.current` in LATER tests — always `await act(async () => ...)`. Future frontend tasks should run `npm test` alongside tsc/lint/build.
- 2026-07-07 — Connectors epic built (`requirements-specs/connector_tasks/`, tasks #1+#3–#7; full detail in that INDEX's progress log). New: `connection_service.py`, `connector_catalog.py` (types registry + validation + `redact_config`), `connector_tasks.py` (beat health checks, rate-limited), `TestConnectionResult` diagnostics across `base.py` + all 5 drivers, discover/restore/hard-delete endpoints, `tests/connectors/` (69 tests). Critical hotfix folded in: `GET /connectors/` was unauthenticated and returned plaintext passwords — all connector routes now auth-gated, all responses redact secrets. Verified: pytest 260/260, frontend tsc/build/vitest clean (lint 29 vs 30 baseline), live curl pass on every new endpoint after image rebuild. **Uncommitted.** Gotchas: (1) live dev Postgres needed manual additive `ALTER TABLE connections ...` (create_all doesn't alter existing tables) — SQL recorded in the connector INDEX progress log; (2) `docker exec` without `-i` silently ignores heredoc stdin — the first "migration" run was a no-op that reported success; (3) partial unique index (`sqlite_where`/`postgresql_where`) is how soft-delete + unique-name coexist, plain `unique=True` on the column must be removed; (4) seeded admin login is `admin@dataplane.ai` / `ADMIN_DEFAULT_PASSWORD`.
- 2026-07-07 — User report "Schema Mapper shows 28 pending AI suggestions on a published mapping; Accept 409s" fixed as a suggestion-lifecycle gap. Product call: **no historical suggestions on published mappings**. (a) `MappingService.publish` now supersedes all pending `ai_suggestions` in the same transaction (new status `superseded`; audit payload gains `suggestions_superseded`); (b) `suggest_mappings_task` re-runs now skip targets with a pending suggestion and never re-offer a rejected (source→target) pair (filtered before best-match selection so the next-best source can still win); (c) mapper `page.tsx` renders `SuggestionPanel` only when `mapping.status === "draft"` — it was the only panel gating on role alone, not `canEdit`. One-time SQL backfill superseded the 28 zombies (actually 14 columns × 2 duplicate task runs; `decided_by='backfill:publish-supersede'`). Verified: pytest 264/264 (4 new, incl. worker-task tests in `tests/mapping/test_suggest_task.py` using a no-close session proxy), vitest 35/35, tsc/build clean, lint 29 baseline, images rebuilt + live curl (0 pending, accept still 409s). **Uncommitted.**
- 2026-07-15 — Second validation pass over the already-built v3 (Agentic DBA Copilot), v4 (ACI integration), v5 (KSM/AES vaulting) epics. Raised per-epic `bugs2.md` + `enhancements2.md` (the first pass's `bugs.md`/`enhancements.md` stand); each epic's INDEX progress log has the authoritative entry. Fixed 10 defects + documented 2, all with regression tests; backend `pytest` 811/811. **v3:** uniqueness_ratio divided a distinct-scan-capped numerator by the full row count → UNIQUE/DEDUPE DQ rules silently never fired above the 100k scan cap (now `min(row_count, scanned_rows)`); LLM column `type` bypassed grounding validation into DDL (now `_TYPE_RE`-validated, rejects the adaptation); schema-design requests naming a SaaS domain ("create target tables for jira data") misrouted to the ACI queue (removed external_action `+1` bonus, schema_design now wins ties priority 20>10); double-approval race (plan row `SELECT…FOR UPDATE`). **v4:** `AciNotConfigured` counted as a breaker failure so the clear "not configured" error degraded to "circuit open" (`_get_client()` moved outside the breaker — same fix applied to the keeper adapter); external-action target resolution mis-routed ticket requests with a cc'd email or `#issue-number` (ticket verb+noun now wins; `#channel` must be letter-led); GET notification-settings not admin-gated; notify_out_task double-commit on poisoned session (rollback first). Documented: notify-out dispatched before caller commit (needs after-commit hook); SQLite type-change collision reported `applied` though not executed. **v5:** retrieve-audit `TTLCache` not thread-safe + accessed outside try/except → concurrent race could break credential resolution on the *default* aes256 backend (added lock, moved inside try); keeper breaker tripped by benign "record not found" (now decided outside the breaker); unknown `SECRET_MANAGER_BACKEND` silently stored plaintext (boot-time `field_validator` + `secret_manager_enabled()` raises). Enhancements logged (not built): AES rekey/re-encrypt endpoint to actually close a key-rotation window; keeper backfill per-row commit (currently non-idempotent on partial failure → orphaned records); async/no-retry external-action discovery off the /ask request path; notification event-key allow-list. Tenant-isolation (v3#11/v4#10/v5#10) still blocked by design — not reclassified as bugs. **Uncommitted.**
- 2026-07-21 — Closed the E09–E16 build-validation pass: 8 genuine defects fixed, 3 findings disproved/already mitigated, and 1 stale E11 finding superseded. Verified backend 984/984, frontend 265/265, and production build clean. Repository-wide lint still has 5 unrelated baseline errors in `ExportModal.tsx` and tenant UI files; the E09–E16 bug-pass files introduce none of them. Full dispositions live in `requirements-specs/enterprise_v2/build_validation_bugs_E09_E16.md`.
- 2026-07-29 (same day, later) — **Per-user connection ownership** — tenant-isolation vertical slice #1 (`requirements-specs/v1.1/07_tenant_isolation_foundation.md` is the full 6-wave application-wide spec; this is a deliberately scoped-down first slice, per user decision: connections-only, plain per-user ownership with no Tenant/Organization model, no Alembic). Root cause: `DBConnection` had no owner column at all, so the demo-data feature above was actually global/shared — any user's "Load Demo Data" was visible to every other user. Added `DBConnection.owner_email` (string, matching the codebase's existing actor convention — `created_by`/`updated_by`/`record_audit`'s `actor` — rather than introducing a new integer FK), backfilled via the same guarded-`ALTER TABLE` pattern already in `main.py`'s `lifespan()` (existing rows → their `created_by`, orphans → the bootstrap admin), and widened the partial unique index `uq_connection_name_active` from `(name)` to `(owner_email, name)` so two users can reuse the same connection name. `ConnectionService`'s every method now takes `owner_email: Optional[str] = None` (`None` = admin bypass — `role=="admin"` sees/manages everyone's connections; every other role is strictly limited to its own, 404 not 403 on someone else's ID to avoid leaking existence) via a new `_owner_scope(user)` helper in `connectors.py`. `demo_data_service.py`'s load/unload/status became per-owner too — each user's "Load Demo Data" now creates *their own* 7 connection rows (pointing at the same shared, idempotent physical sample files under `/shared/data/` — no need to duplicate the files, just the ownership records). **Explicit residual risk, not fixed by this slice**: pipelines/mappings/query/query-studio/schema-discovery still reference a `connection_id` with zero ownership check — a user who already knows/guesses another user's connection ID can still use it from those still-unscoped routers; closing that is Wave C of the full spec, future follow-up. Verified: backend pytest 1063/1067 (same 4 pre-existing unrelated Entra env-leak failures as before, confirmed via `git stash`), new isolation tests in `tests/connectors/test_connection_service.py` and `tests/connectors/test_connectors_router.py` (two-owner-same-name, 404-not-403 across owners, admin bypass) plus a new `tests/connectors/test_demo_data_service.py`, frontend build clean (no frontend files touched — `owner_email` is just an additive optional response field), 68/68 connectors/dashboard vitest. Live-verified end-to-end on staging with two real throwaway accounts after rebuilding/restarting the `api` container: user A's created connection + loaded demo data were both invisible to user B (404/empty list), admin saw both with the correct `owner_email` on each row — the exact scenario that prompted this work. No `Tenant`/`Organization` model exists yet; if the product later needs users to share data with named co-workers, that's the full spec's Wave A, not this slice.
- 2026-07-29 — **Demo data is now opt-in** (was previously force-seeded on every startup — every login saw 6 sample DB connections mixed in with real data, no way to turn it off). Moved the 5 physical-SQLite seed helpers (`_seed_crm_source` etc.) out of `main.py`'s `lifespan()` verbatim into new `app/services/demo_data_service.py` (`load_demo_data`/`unload_demo_data`/`is_demo_data_loaded`, all idempotent — file/row-exists guards unchanged) and a new `app/api/routers/demo_data.py` (`GET/POST /api/v1/demo-data/status,load`, `DELETE /api/v1/demo-data`, any authenticated user — no tenant scoping exists in this app today, so this is one **global** on/off switch, not per-user, by explicit product decision). `lifespan()` no longer seeds sample data or DBConnection rows at all — only tables/migrations, the bootstrap admin user, Celery schedules, and RBAC. Frontend: new `dashboard/components/DemoDataBanner.tsx` (status via `useWidgetData`, Load/Remove actions) rendered at the top of `dashboard/page.tsx`. **Login page**: restructured so "Sign in with Microsoft" (Entra SSO — already fully wired, enabled in this env with `ENTRA_AUTO_PROVISION_DOMAIN=veltris.com`) is now the primary CTA; the email/password form is collapsed behind a "Sign in with a different email" toggle when Entra is enabled (unchanged, always-visible when it isn't). Verified: backend pytest 1052/1056 (4 failures are a pre-existing env leak in `tests/auth/test_entra_login.py` — this container's real `ENTRA_AUTO_PROVISION_DOMAIN` collides with the test's hardcoded fixture user; reproduced identically via `git stash` on unmodified code), frontend production build clean (25/25 routes), vitest 73/73 for every login/dashboard suite (13 pre-existing unrelated `QueryWorkspaceInner`/`useRouter`-mock failures, also confirmed via `git stash`), lint clean on every touched file (baseline-only errors remain in untouched `tenants/` components). **Uncommitted.**
- 2026-08-01 (later) — **Mapping-suggestions precompute** — brainstormed/spec'd/planned/implemented in one session (design: `docs/superpowers/specs/2026-08-01-mapping-suggestions-precompute-design.md`). Goal: warm AI mapping suggestions in the background when a mapping is created instead of the user paying local-Ollama latency synchronously on "Get Suggestions". Reused the existing Autopilot governance system rather than a bespoke trigger: added a 4th evaluator `AutopilotEngine._evaluate_new_draft_mappings` (draft mappings with `pending_suggestion_task_id IS NULL`) that proposes the *same* `mapping_suggestions_refresh` action already used by schema-drift — already `auto_capable=True` in the registry, so no registry change, and behavior is entirely gated by the existing `AutopilotPolicy` autonomy setting for that action type (still defaults to `suggest`, not `auto` — the actual zero-latency win requires an operator to explicitly flip it, flagged in the spec, not yet done). `MappingService.create_mapping` now dispatches a guarded `evaluate_recommendations_task.delay()` mirroring `connector_tasks.py`'s post-health-check pattern. Non-obvious fix along the way: `upsert_recommendation` overwrites confidence/rationale on same-subject refresh (last-processed-evaluator wins), so a mapping that's both brand-new and drift-affected in one sweep would silently get whichever evaluator ran last — replaced implicit list-order reliance with an explicit `_merge_drafts_by_subject` priority function (`schema_drift` > `mapping_created`) so the outcome is deterministic and independent of evaluator call order/future additions. Verified: full backend suite 1086/1086 passing (one new dispatch test initially failed from an Edit-tool mistake that orphaned a trailing assertion into the wrong test function — caught and fixed before commit). No frontend or schema changes. **Uncommitted.**
- 2026-08-01 — **ECC token-optimization audit** — tailored `ecc_token_optimizer_prompt.md` with this repo's actual stack (Python/FastAPI/Celery backend, Next.js/React/TypeScript frontend) and ran it. Findings that reversed the original assumptions: ECC's global `rules/` dir (`~/.claude/plugins/marketplaces/ecc/rules`) is shipped-but-never-installed source, not something to delete — installed a project-scoped subset instead (`.claude/rules/ecc/{common,python,typescript,react,web}`, first rules DataOne has ever had). The selective-install CLI (`install-plan.js`/`install-apply.js`) turned out unable to produce a truly stack-filtered project install: `framework-language` (the one module that filters by language) hard-depends on `rules-core`, which always ships all 22 language dirs regardless of filter, and project-local skill/agent installs are additive on top of the global plugin, not a replacement — so a project-level install can't reduce what a session actually sees. The only change that lowers real per-session context load is trimming the **global** plugin directly: removed 157 of 281 skills and 26 of 67 agents (stack/domain-irrelevant — other-language stacks, unrelated business verticals, unused third-party ops integrations) from `~/.claude/plugins/marketplaces/ecc`, committed by the user. Measured effect: skill-listing description chars 59,605→27,436 (−54%), agent-listing 14,718→9,139 (−38%) — this is global, affects every project using this ECC install, not just DataOne. Also fixed a broken prerequisite (`node_modules`/`ajv` was never installed in the plugin dir) and added `.claude/settings.local.json` env vars (`ECC_HOOK_PROFILE=standard`, `ECC_DISABLED_HOOKS=pre:bash:tmux-reminder` — Docker-first repo, no tmux workflow, and CI is deploy-only with no lint/test gate so local Stop hooks stay the real safety net).
- 2026-07-31 — **Pipeline-failure Autopilot escalation** — sub-project 1 of a 4-part roadmap to extend the existing Autopilot evaluate→recommend→guardrailed-execute loop beyond connector-health/schema-drift (design: `docs/superpowers/specs/2026-07-30-pipeline-failure-autopilot-escalation-design.md`; DQ/Governance/Risk remain future roadmap items, not built here). Correction made during design: pipeline auto-retry-on-transient-failure already existed (`app/workers/pipeline_tasks.py`) — this only adds the missing escalation layer on top of an existing passive `pipeline:run_failure` notification. Added `AutopilotEngine._evaluate_pipeline_failures` (`autopilot_engine.py`) — latest-`PipelineRun`-per-`pipeline_id` via the same `func.max(id)` grouped-subquery pattern the schema-drift evaluator uses, filtered to `status=="failed"`, drafting up to 3 recommendations: the existing `connector_health_check` (reusing the *same* subject convention as the connector-health evaluator so they dedupe onto one row), the existing `notify_slack_internal`, and a new `pipeline_schedule_disable` action (only drafted when the pipeline has an enabled schedule). Extended `_supersede_cleared` so a pipeline's later successful run clears its open notify/schedule-disable recs. New `pipeline_schedule_disable` `ActionSpec` in `autopilot_registry.py` (risk=medium, reversible=True, `auto_capable=False` — pausing future runs is a judgment call even though trivially reversible). **Bug found and fixed during implementation**: the first draft's executor called `PipelineCRUD.toggle_schedule`, which commits internally and raises `HTTPException` — violating this codebase's documented executor contract (bugs/01: "executor callables NEVER commit the caller's session") that `ConnectionService.update_health` (used by `_exec_connector_health_check`) explicitly follows ("stages only — the caller owns the commit"). Fixed by staging the mutation directly (`pipeline.schedule.enabled = False; db.flush()`) instead of calling the router-facing helper — avoids a redundant `pipeline_schedule_toggled` audit event alongside `execute_recommendation`'s own `autopilot_action_executed` event, and a regression test now locks in exactly one audit event on this path. Also had to install `msal==1.31.0` into this repo's own `backend/.venv` to run tests locally at all — that venv appears to be a partial copy of a sibling `dataplane-main` checkout (its `pip`/`pytest` shebangs hardcode that other path) and was missing the dependency entirely; not otherwise related to this change. **Second bug found by the code-review pass (`ecc:code-reviewer`) and fixed**: the `connector_health_check` leg was drafted unconditionally on every failed run, regardless of the source connection's cached `health_status`. Since `_supersede_cleared`'s pre-existing rule for that action type closes any open `connector_health_check` the instant `health_status == "healthy"` — correct for its original producer (`_evaluate_connector_health`, which only ever drafts when already degraded/down) — a connection that was already cached `"healthy"` (the common/default case) got its freshly-drafted rec created and superseded in the very same transaction, then recreated and re-superseded every subsequent 2-minute sweep for as long as the pipeline kept failing: unbounded audit-log growth and a notify-out fire every tick, with zero actual value delivered. Fixed by skipping the `connector_health_check` draft entirely when `pipeline.source_connection.health_status == "healthy"` (loaded via an added `joinedload`) — a connection already believed healthy gets no marginal benefit from this proactive re-test. New regression test `test_healthy_connection_does_not_get_a_churning_health_check_draft` locks in zero drafts and zero supersedes across two consecutive sweeps in that state. Verified: new `tests/autopilot/test_pipeline_escalation.py` + additions to `tests/autopilot/test_registry.py` (dedupe with the existing connector-health evaluator, no-schedule/already-disabled skip cases, newest-run-only, supersede-on-recovery, approve→execute→schedule-actually-disabled end-to-end, healthy-connection-no-churn), full backend suite **1080/1080**. **Uncommitted.**
