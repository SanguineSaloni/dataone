# Task 01 — Rename the product to DataOne

Replace user-facing `dataPlane` branding in the frontend with `DataOne`, including metadata, landing, login, and dashboard chrome. Backend identifiers, database names, compatibility keys, and historical documentation remain unchanged.

## Acceptance criteria

- The browser title and primary product surfaces say `DataOne`.
- Existing API routes and storage compatibility are unchanged.
