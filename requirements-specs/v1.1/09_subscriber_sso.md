# Task 09 — Subscriber SSO Identity Lane

Implement verified multitenant Entra OIDC for provisioned subscriber organizations. Validate token claims server-side, resolve `(tid, oid)` to an active tenant membership, enforce subscription entitlement, and issue the DataOne application session.

Include admin-consent onboarding where required, just-in-time access only under an explicit tenant policy, account recovery without email-based linking, logout/session revocation, and negative cross-tenant tests.

## Status

`[!]` Blocked by Task 07 and Partner Center/Entra registration inputs.
