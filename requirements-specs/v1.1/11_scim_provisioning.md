# Task 11 — SCIM Subscriber Lifecycle

Optionally implement a SCIM 2.0 endpoint so subscriber Entra administrators can provision/deprovision users and groups. SCIM membership controls user availability and roles; Marketplace entitlement remains the organization-level commercial access control.

Require bearer credential rotation, tenant-bound SCIM endpoints, idempotent operations, group-to-role mapping, deprovisioning session revocation, rate limits, and audit history.

## Status

`[?]` Product decision: include for initial Marketplace release or schedule as the enterprise onboarding follow-up.
