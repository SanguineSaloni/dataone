[pytest]
# Allow `from app...` imports from backend/tests/* conftest and test files.
# pytest auto-discovers tests; pythonpath makes the `app` package importable
# regardless of which directory pytest is invoked from.
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning
